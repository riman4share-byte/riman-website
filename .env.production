# ============================================
# PRODUCTION BUILD CONFIG — THIS FILE IS COMMITTED ON PURPOSE.
# Cloudflare Pages builds straight from this repo; these values are PUBLIC
# by design (they are compiled into the JS bundle every visitor downloads).
#
# NEVER PUT A SECRET HERE. Anything without a VITE_ prefix is read at BUILD
# TIME ONLY (loadEnv) — still keep real secrets in Cloudflare dashboard or
# Supabase function secrets. If in doubt, it does not go in this file.
# ============================================

# Canonical site URL: used for SEO canonicals, sitemap, og tags + it is the
# allowlist for checkout success/cancel URLs server-side.
SITE_URL=https://riman.ae

# Supabase project (anon key is safe ONLY because RLS enforces authorization).
VITE_SUPABASE_URL=https://vbuavhnpemnfsuguglqn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZidWF2aG5wZW1uZnN1Z3VnbHFuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODczNzI0ODUsImV4cCI6MjEwMjk0ODQ4NX0.AVYtt91iVpGIiII7L2ZYISI5d-4S5vvafOvQxQAwbS0

# Edge function endpoints are DERIVED from VITE_SUPABASE_URL automatically
# (functions/v1/create-checkout, create-order). Uncomment only to override:
# VITE_STRIPE_CHECKOUT_ENDPOINT=
# VITE_CREATE_ORDER_ENDPOINT=
